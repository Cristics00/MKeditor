# MK编辑器 设计思路

## 一、项目定位

一款轻量级、开箱即用的 Windows 桌面 Markdown 编辑器，面向日常写作与笔记场景。核心理念：**功能够用、界面干净、单文件交付**——整个应用集中在一个 Python 文件（约 1200 行），降低维护成本。

## 二、技术选型

| 层面 | 方案 | 理由 |
| --- | --- | --- |
| 界面框架 | PyQt5（Fusion + QSS） | 成熟稳定，QSS 可实现 GitHub 风格深浅双主题 |
| Markdown 渲染 | `markdown` 库（extra 扩展）+ Qt 富文本引擎 | 无浏览器依赖即可渲染 HTML 预览 |
| 数学公式 | KaTeX（本地资源，数学片段先摘出再还原） | 支持 LaTeX 公式渲染 |
| 打包 | PyInstaller（onedir 方式） | 产物结构透明、启动快，改代码后只需替换 exe |

## 三、整体架构

单文件内按职责划分为四个层次：

1. **工具函数层**：数学公式摘取/还原、预览 CSS 生成、圆角菜单、深色标题栏（DWM API）、资源路径定位等。
2. **编辑器组件层**：
   - `MarkdownHighlighter`（QSyntaxHighlighter）——深浅两套配色的语法高亮；
   - `LineNumberArea` + `EditorPane`（QPlainTextEdit）——行号栏、当前行高亮、Ctrl+滚轮缩放。
3. **主窗口层** `MainWindow`（QMainWindow）：双页签（编辑/预览）+ 工具栏 + 菜单 + 状态栏，集中承载文件操作、查找、缩放、主题切换等交互逻辑。
4. **入口层** `main()`：初始化应用、配置 QtWebEngine、处理命令行传入的文件路径。

## 四、核心设计决策

- **编辑/预览分离为两个页签**而非分屏实时同步：实现简单、干扰少，用 F5 手动刷新预览，避免频繁重渲染带来的卡顿。
- **预览缩放的后处理方案**：Qt 富文本引擎会忽略标题的 CSS 字号，因此在 HTML 渲染前对标题注入整数 px 内联样式、对图片按缩放系数注入宽高，实现 50%~300% 的整体缩放。
- **数学公式的「摘取—渲染—还原」**：先用占位符把公式从正文中取出，避免 markdown 库破坏 LaTeX 语法，渲染后再还原给 KaTeX 处理。
- **编码兼容**：读取文件时自动识别 UTF-8 / GBK，照顾中文 Windows 环境。
- **未保存保护**：修改后标题栏加 `*` 标记，切换页签刷新预览前、关闭程序前均做丢弃确认。

## 五、界面与体验

- 浅色（GitHub 风）/ 深色（GitHub Dark 风）一键切换，标题栏配色在 Win10/11 下同步跟随；
- 细节打磨：圆角下拉菜单、圆角滚动条、图标化工具栏带悬浮提示、自定义程序图标；
- 快捷键覆盖高频操作（新建/打开/保存、查找、加粗/斜体、缩放、切主题）。

## 六、质量保障

配套三个测试文件：`test_math.py`（公式摘取还原）、`test_theme.py`（主题切换）、`test_e2e.py`（端到端流程），以自动化测试守护核心逻辑。

## 七、可扩展方向

- 分屏实时预览、大纲导航、导出 PDF/HTML；
- 多文档（多页签文件）支持；
- 配置持久化（记住主题、字号、窗口大小）。
